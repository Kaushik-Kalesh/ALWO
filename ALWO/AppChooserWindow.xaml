<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ALWO.AppChooserWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ALWO"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Choose Apps"
    Closed="Window_Closed">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <ScrollView>
        <StackPanel Orientation="Vertical">
            <ListView Name="AppsListView" SelectionMode="Multiple">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon}" Margin="0, 0, 8, 0" Height="20" />
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Click="DoneButton_Click" Content="Done" Margin="0, 8, 0, 8" HorizontalAlignment="Center" />
        </StackPanel>
    </ScrollView>
</Window>
